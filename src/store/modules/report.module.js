import ApiService from "@/common/api.service";
// import { FETCH_USERS, CREATE_USER, UPDATE_USER, CHANGE_PASSWORD } from "../actions.type";
// import { SET_USERS } from "../mutations.type";

const state = {
  // users: []
};

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
  state,
  getters,
  mutations,
  actions
};
